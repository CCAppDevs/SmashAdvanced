@model IEnumerable<SmashAdvanced.Models.GameTag>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Game)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tag)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Game.GameId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tag.TagId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GameTagId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GameTagId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GameTagId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
